package cn.springboot.proxy;

import java.lang.reflect.Proxy;

public class DemoMain {

	/*public static void main(String[] args) {
		Demo demo = new DemoImpl();
		ProviderHandler providerHandler = new ProviderHandler(demo);
		Demo demoProxy = (Demo) Proxy.newProxyInstance(demo.getClass().getClassLoader(), demo.getClass().getInterfaces(),
				providerHandler);
		demoProxy.run();
	}*/
}
